//import * as nock from 'nock';
//import { baseURL } from '../../test/test-common';

// export const mockGetItemsRequest = (siteId, reply) => {
//   return nock(baseURL, {
//     reqheaders: {
//       Authorization: `MSID.${siteId}`,
//     },
//   })
//     .get('/_api/whats-next-web/items?realEstateId=whatsNext&allowedHost=editor')
//     .reply(200, reply);
// };
